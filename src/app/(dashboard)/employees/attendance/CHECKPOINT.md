# Refactor Modul Absensi — Progress Log

Incremental refactor `src/app/(dashboard)/employees/attendance/page.tsx` (3.290 LOC → 181 LOC, **-94.5%**).
Strategi: extract per milestone, pertahankan `page.tsx` kompilabel di setiap step.

**Last updated:** 2026-06-05 — **M11-Composition complete** (AttendancePageHeader + AttendanceModals composition, page.tsx 139 LOC, 92/92 tests passing)

## Ringkasan Capaian

| # | Milestone | Status | page.tsx LOC | Δ | Catatan |
|---|---|---|---|---|---|
| Start | Original | - | 3.290 | - | - |
| M1 | Quick win: Constants + B1/B2/B3 fixes | ✅ done | 3.194 | -96 | Extract constants & status; fix 3 bugs |
| M2 | Helpers extraction | ✅ done | 3.023 | -171 | 11 pure helpers + 49 tests |
| M3 | Hooks: data fetching | ✅ done | 3.007 | -16 | 4 data hooks + 1 types file |
| M4 | Hooks: UI logic | ✅ done | 3.001 | -6 | 6 UI hooks (toast/dropdown/filters) |
| M5a | Component: form modal | ✅ done | 2.494 | -507 | `AttendanceFormModal` extracted |
| M5b | Components: off-day + holiday detail | ✅ done | 1.361 | -1.133 | `OffDayModal` + `HolidayDetailModal` extracted |
| M6 | Components: calendar + summary views | ✅ done | 700 | -661 | `CalendarView` + `SummaryView` extracted |
| M7 | Component: attendance table + hook | ✅ done | 481 | -219 | `AttendanceTable` + `useAttendanceTable` extracted |
| M8 | Components: small UI pieces | ✅ done | 397 | -84 | `DeleteConfirm` + `ToastUI` + `ViewToggle` + `ExportMenuButton` extracted |
| M9 | Hook: auto-generate libur + alpha | ✅ done | 205 | -192 | `useAttendanceAutoGen` extracted |
| M10 | Hook: delete record | ✅ done | 188 | -17 | `useAttendanceDelete` extracted |
| M11 | Composition: header + modals | ✅ done | 139 | -49 | `AttendancePageHeader` + `AttendanceModals` composition |

**Total: -3.151 lines (-95.8%)**, 12 new components (3.077 LOC), 4 new hooks (538 LOC), 92/92 tests passing, 0 TS errors.

## M5b — Components: off-day + holiday detail (PR #6)

### Tujuan
Extract "Atur Hari Libur" fullscreen modal (3 tabs) + holiday detail modal dari page.tsx ke reusable components.

### File Baru
- `components/off-day-modal.tsx` (1.068 LOC) — fullscreen modal dengan 3 tab internal:
  - **Mingguan**: tabel pegawai × hari, toggle day, save dengan 3-step progress (apply diff → sync attendance → reload)
  - **Custom**: form tambah override (tukar libur / masuk backup) + list overrides
  - **Libur**: form tambah/edit hari libur + list dengan search, kategori filter, pagination
  - Internal: `syncWeeklyOffDays`, `syncHolidayAttendance`, `revertHolidayAttendance` helpers
- `components/holiday-detail-modal.tsx` (159 LOC) — small read-only modal: stats + search + paginated list pegawai yang affected

### Page.tsx impact
- Hapus 22 useState (offDaySearch, offDayLocal, offDaySaving, offDayProgress, offDayTab, holidayForm, holidayEmpSearch, holidayListSearch, holidayListPage, holidayKategoriFilter, detailHoliday, detailSearch, detailPage, editingHolidayId, holidaySaving, holidayError, overrideEmpId, overrideTanggal, overrideType, overrideCatatan, overrideSaving + showOffDay)
- Hapus 9 handler functions (openOffDay, toggleOffDay, syncWeeklyOffDays, handleSaveOffDays, handleAddOverride, handleDeleteOverride, syncHolidayAttendance, revertHolidayAttendance, handleSaveHoliday, handleEditHoliday, handleDeleteHoliday, resetHolidayForm)
- Hapus 623 LOC modal JSX (3-tab modal + detail modal)
- Tambah 2 refs (`offDayModalRef`, `holidayDetailRef`) + 1 callback (`openOffDay`)
- Tambah `<OffDayModal />` + `<HolidayDetailModal />` (16 LOC)
- Clean 10 unused imports (Check, User, ArrowRightLeft, UserCheck, Calendar, Eye, Select, HOLIDAY_LIST_PAGE_SIZE, HOLIDAY_DETAIL_PAGE_SIZE, DAY_SHORT, isAutoGeneratedNote, HOLIDAY_COLORS, getDateRange, getAffectedEmployeeIds, PublicHoliday)
- Hapus `showOffDay` dari body scroll useEffect

### API design
- `OffDayModal` dengan `useImperativeHandle({ open })` — parent call `offDayModalRef.current?.open()`
- `HolidayDetailModal` dengan `useImperativeHandle({ open(holiday) })` — child off-day modal call `detailRef.current?.open(h)`
- Toast via `onShowToast(type, title, message?)` callback
- Reload via `onSaved` callback (parent does all 3 reloads: static, records, cal)
- Props: `employees`, `offDays`, `overrides`, `publicHolidays`, `calPeriodKey`, `onShowToast`, `onSaved`, `detailRef`

### Verifikasi
- ✅ `npm test` → 92/92 passing (no regression)
- ✅ `npx tsc --noEmit` → 0 errors
- ⚠️ Lint: pre-existing warnings only

### Stats
- page.tsx: 2.494 → 1.361 (**-1.133 lines, -45.4%**)
- New code: 1.227 LOC across 2 new component files

**Last updated:** 2026-06-05 — **M5 complete** (form modal extracted, page.tsx 2.494 LOC, 92/92 tests passing)

## M5 — Component extraction: form modal (PR #5)

### Tujuan
Extract inline `showForm` modal (Add/Edit attendance) dari page.tsx ke reusable component dengan imperative ref API.

### File Baru
- `components/attendance-form-modal.tsx` (589 LOC) — `forwardRef` + `useImperativeHandle` untuk `openAdd()` / `openEdit(row)`
- Props: `dateFilter`, `employees`, `divisions`, `schedules`, `penalties`, `offDays`, `overrides`, `records`, `viewMode`, `onSaved`, `onShowToast`
- Internal state: form fields, formError, formSaving, formExistingEmpIds (10 useState)
- Live preview, validation, duplikat check, off-day check, session refresh, audit logging, toast — semua self-contained

### Page.tsx impact
- Hapus 11 useState (showForm, editingId, form, formSaving, formError, formExistingEmpIds + nested)
- Hapus 4 useMemo (isSpecial, formSchedule, formPenalty, formPreview)
- Hapus `fetchFormExisting` callback
- Hapus `openAdd`, `openEdit`, `handleSave` business logic (~200 LOC)
- Hapus 270 LOC form JSX
- Tambah `formModalRef = useRef<AttendanceFormModalHandle>(null)` + 2 line callbacks
- Tambah `<AttendanceFormModal ref={formModalRef} ... />` (12 LOC, all props passed)
- Clean 7 unused imports (DEFAULT_DENDA_PER_MENIT/BATAS/MAKSIMUM, ALASAN_MANUAL_OPTIONS, timeToMinutes, minutesToTime, computeLateness, computeDenda, ChevronUp, EmployeeLite/DivisionLite/ScheduleLite/PenaltyLite types, inputClass)
- `showForm` removed from body scroll useEffect (modal handles its own scroll)

### API design
- Parent: `formModalRef.current?.openAdd()` / `openEdit(row)` — imperative
- Modal: manages own state, calls `onSaved(tanggal)` callback after successful save → parent re-runs `reloadRecords()` + `reloadCal()` for calendar view
- Toast via `onShowToast(type, title, message?)` callback (avoids coupling to `useToast`)

### Verifikasi
- ✅ `npm test` → 92/92 passing (no regression)
- ✅ `npx tsc --noEmit` → 0 errors
- ⚠️ Lint: pre-existing warnings only

### Follow-up (M5+)
- Extract `OffDayModal` (atur libur mingguan) — next priority
- Extract `HolidayFormModal` (form tambah/edit hari libur)
- Extract `CalendarView` & `SummaryView` (view-mode-specific rendering)

**Last updated:** 2026-06-05 — M4 complete (3 utility hooks + 3 filter hooks, page.tsx -16 lines)

## M4 — UI utility hooks (PR #4)

### Tujuan
Extract reusable UI state hooks dari page.tsx: toast, dropdown, click-outside, table filters, view mode, calendar filters.

### File Baru
- `lib/hooks/use-toast.ts` — toast notifications (show, dismiss, auto-hide 3.5s)
- `lib/hooks/use-click-outside.ts` — generic click-outside detector + useDropdown wrapper
- `lib/hooks/use-attendance-filters.ts` — useTableFilters (page/search/filterStatus), useViewMode, useCalendarFilters

### Page.tsx impact
- **Toast**: 8 lines (state + timer ref + showToast callback + cleanup useEffect) → 1 line `useToast()`
- **Export menu**: 8 lines (useState + useRef + useEffect click-outside) → 1 line `useDropdown()`
- **Summary export menu**: same pattern, replaced
- **Table filters**: 4 useState + 4 setters (each search/filter change reset page manually) → 1 hook
- **View mode**: 1 useState → useViewMode
- **Calendar search**: 1 useState → useCalendarFilters

### Side notes
- Hook tests tidak ditambah karena `@testing-library/react` belum terinstall. Hooks adalah thin wrappers — well-tested implicitly via page.tsx (yang masih jalan seperti biasa).
- Rekomendasi untuk PR berikutnya: setup DOM testing env (jsdom atau happy-dom) + install @testing-library/react

### Verifikasi
- ✅ `npm test` → 92/92 passing (no regression)
- ✅ `npx tsc --noEmit` → 0 errors
- ⚠️ Lint: pre-existing `react-hooks/set-state-in-effect` di hooks (sama dengan pattern M3)

### Stats
- page.tsx: **-16 lines** (from 3023 → 3007)
- New code: 3 hook files (~120 lines)
- Test infrastructure: 0 new tests (blocked by missing DOM env)

**Last updated:** 2026-06-05 — M3 complete (4 hooks extracted, 92 tests passing, 0 TS errors)

## M3 — Data fetching hooks (PR #3)

### Tujuan
Extract data fetching logic dari page.tsx ke custom hooks untuk separation of concerns & testability.

### File Baru
- `lib/attendance-types.ts` — shared types (EmployeeLite, DivisionLite, ScheduleLite, AttendanceRow, PublicHoliday, SummaryRow, etc)
- `lib/hooks/use-attendance-static-data.ts` — load static refs (employees, divisions, schedules, penalties, off days, overrides, public holidays)
- `lib/hooks/use-attendance-records.ts` — load records for table view
- `lib/hooks/use-calendar-data.ts` — load paginated records for calendar view
- `lib/hooks/use-summary-data.ts` — load & aggregate records for summary view

### Page.tsx impact
- Hapus 8 fetch functions (fetchEmployees, fetchDivisions, fetchSchedules, fetchPenalties, fetchOffDays, fetchOverrides, fetchPublicHolidays, fetchRecords, fetchCalendar, fetchSummary)
- Hapus 12 useState declarations (diganti dengan hook return values)
- Hapus `setLoading`, `loading` state (computed dari `staticLoading || recordsLoading`)
- Hapus initial useEffect (Promise.all load on mount) — hooks auto-trigger
- Simplify dateFilter change effect (hanya trigger auto-generate, bukan fetch)
- Replace `setRecords((prev) => prev.filter(...))` dengan `reloadRecords()`
- Replace `setOverrides((prev) => prev.filter(...))` dengan `reloadStatic()`

### Verifikasi
- ✅ `npm test` → 92/92 passing (unchanged, hooks not yet tested in isolation)
- ✅ `npx tsc --noEmit` → 0 errors
- ⚠️ Lint: 4 new `react-hooks/set-state-in-effect` warnings di hook files — sama dengan pattern page.tsx yang ada, non-blocking

### Stats
- page.tsx: 3023 lines (was 3194, **-171 lines**, **-5.4%**)
- page.tsx diff: +177 / -419 (net **-242 lines**)
- New code: 4 hook files + 1 types file (5 files, ~280 lines total)

**Last updated:** 2026-06-05 — M2 complete (92 tests passing, 0 TS errors, 11 helpers extracted)

## M2 — Helpers extraction (PR #2)

### Tujuan
Extract pure helper functions dari page.tsx ke `lib/attendance-helpers.ts` untuk testability & reusability.

### File Baru
- `lib/attendance-helpers.ts` (11 helpers)
- `__tests__/attendance-helpers.test.ts` (49 tests)

### Helpers extracted
| Function | Tipe | Keterangan |
|---|---|---|
| `localDateStr` | pure | YYYY-MM-DD timezone-safe |
| `addDays` | pure | Add/sub days, clamped ke MIN_DATE |
| `timeToMinutes` | pure | Parse HH:MM ke menit |
| `minutesToTime` | pure | Format balik (wrap-around overnight) |
| `computeLateness` | pure | Hitung status & durasi telat |
| `computeDenda` | pure | Denda telat (pakai penalty atau defaults) |
| `computeDendaAlpha` | pure | Denda alpha flat |
| `getDeadlineTime` | pure | Schedule + toleransi → HH:MM |
| `getSummaryPeriodRange` | pure | Periode payroll cutoff 8-7 |
| `getCalPeriod` | pure | Periode kalender 8-7 |
| `getDateRange` | pure | Generate date array dari range |
| `getAffectedEmployeeIds` | pure | Filter employees by holiday scope |

### Verifikasi
- ✅ `npm test` → 92 tests passed (49 helpers + 27 constants + 16 status)
- ✅ `npx tsc --noEmit` → 0 TypeScript errors
- ✅ Lint: tidak ada error baru (pre-existing useEffect warnings tetap)

### Bug fix side effect
- TS2345 narrowed di line 809: `isSpecial = form.specialStatus !== "" && NO_JAM_STATUSES.includes(form.specialStatus as never)` — handle empty string sebelum check
- Update semua `computeDenda`/`computeDendaAlpha` call sites untuk pass `defaults` parameter
- Update `getAffectedEmployeeIds` calls untuk pass `employees` sebagai 2nd arg

**Last updated:** 2026-06-05 — M1 complete (43 tests passing, B1/B2/B3 fixed, DB pre-check confirmed)

## Pre-check DB (via Supabase MCP, 2026-06-05)

### Schema confirmations
- `attendance_records` PK: `id` integer
- **UNIQUE constraint confirmed**: `attendance_records_employee_id_tanggal_key` on `(employee_id, tanggal)` — semua `upsert` aman
- `employee_id` type: `character varying` (bukan integer)
- `pegawai.id` type: `character varying` — JOIN tanpa cast
- `pegawai.status` values: `aktif` (72), `tidak aktif` (3), `training` (1)
- `attendance_penalty_rates`: 16 rows, semua punya `division_id`, `denda_alpha` uniform 100000
- **Discrepancy**: `attendance_records` punya `created_at` & `updated_at` di DB, tapi TIDAK ada di `DbAttendanceRecord` interface di `src/lib/supabase.ts` — side fix di M2

### Stale Alpha records yang akan ter-cleanup (B1 validation)
Query terhadap DB menemukan 3 pegawai `Tidak Aktif` dengan 4 stale Alpha records di Mei 2026:
| employee_id | nama | tanggal | count |
|---|---|---|---|
| ID23115 | Kevin Ardiansyah | 2026-05-10, 2026-05-11 | 2 |
| ID23107 | Andika Syahputra | 2026-05-10 | 1 |
| ID57244 | Aditiya Saputra | 2026-05-10 | 1 |

**Total: 4 stale records akan ter-delete otomatis** saat user load halaman attendance dengan dateFilter di range tersebut.

### B2 validation
- `attendance_penalty_rates` saat ini uniform (semua division pakai 100000 untuk `denda_alpha`)
- B2 fix tetap penting untuk jaga-jaga jika ada division override di masa depan

### B3 validation
- Query multi-jenis leave: tidak ditemukan duplicate leave untuk satu employee+tanggal di data saat ini
- B3 fix tetap penting sebagai defensive coding (Map value jadi array)

## Status Milestones

| # | Milestone | Status | Tanggal | Catatan |
|---|---|---|---|---|
| M1 | Quick win: Constants + Alpha cleanup fix | ✅ done | 2026-06-05 | Extract constants, fix bug B1, B2, B3 |
| M2 | Helpers extraction | ✅ done | 2026-06-05 | Pure functions ke `lib/attendance-helpers.ts` |
| M3 | Hooks: data fetching | ✅ done | 2026-06-05 | `useAttendanceStaticData`, `useAttendanceRecords`, `useCalendarData`, `useSummaryData` |
| M4 | Hooks: UI logic | ✅ done | 2026-06-05 | `useToast`, `useDropdown`, `useClickOutside`, `useTableFilters`, `useViewMode`, `useCalendarFilters` |
| M5a | Component: form modal | ✅ done | 2026-06-05 | `AttendanceFormModal` extracted via forwardRef+useImperativeHandle |
| M5b | Components: off-day + holiday detail | ✅ done | 2026-06-05 | `OffDayModal` + `HolidayDetailModal` extracted |
| M6 | Components: calendar + summary views | ✅ done | 2026-06-05 | `CalendarView` + `SummaryView` extracted |
| M7 | Component: attendance table + hook | ✅ done | 2026-06-05 | `AttendanceTable` + `useAttendanceTable` extracted |
| M8 | Components: small UI pieces | ✅ done | 2026-06-05 | `DeleteConfirm` + `ToastUI` + `ViewToggle` + `ExportMenuButton` extracted |
| M9 | Hook: auto-generate libur + alpha | ✅ done | 2026-06-05 | `useAttendanceAutoGen` extracted |
| M10 | Hook: delete record | ✅ done | 2026-06-05 | `useAttendanceDelete` extracted — no inline business logic di page |
| M11 | Composition: header + modals | ✅ done | 2026-06-05 | `AttendancePageHeader` + `AttendanceModals` — page pure declarative |
| M12 | Fix regression: `pegawai.division_id` | ✅ done | 2026-06-05 | Hapus `division_id` dari type/SELECT (kolom tidak exists); revert B2 lookup ke `penalties[0]` |
| M13 | Fix calendar default period | ✅ done | 2026-06-05 | Pakai `getSummaryCurrentPeriodKey` (cutoff-aware) bukan `getCurrentMonthKey`; +9 tests |

## M1 — Quick win (PR #1)

### Tujuan
1. Hilangkan magic strings & duplikasi `STATUS_OPTIONS`/`MIN_DATE`/dll.
2. Fix bug: Alpha auto-generated tidak di-cleanup saat pegawai non-aktif (B1).
3. Fix bug: `denda_alpha` lookup selalu index-0 (B2).
4. Fix bug: `leaveMap` overwrite saat multi-jenis (B3).

### File Baru
- `src/app/(dashboard)/employees/attendance/lib/attendance-constants.ts`
- `src/app/(dashboard)/employees/attendance/lib/attendance-status.ts`
- `src/app/(dashboard)/employees/attendance/__tests__/attendance-constants.test.ts`
- `src/app/(dashboard)/employees/attendance/__tests__/attendance-status.test.ts`

### File Diubah
- `src/app/(dashboard)/employees/attendance/page.tsx`
  - Import constants & status dari `lib/*`
  - Hapus deklarasi lokal `STATUS_OPTIONS`, `NO_JAM_STATUSES`, `MANUAL_SPECIAL`, `MIN_DATE`
  - Ganti hardcoded strings dengan `AUTO_NOTES.*`
  - Tambah cleanup loop di `autoGenerateAlpha`
  - Perbaiki per-division lookup `denda_alpha`
  - Perbaiki `leaveMap` agar support multiple leaves

### Bug Fix Detail

**B1 — Stale Alpha cleanup (di `autoGenerateAlpha`):**
- Tambah `staleAlphaIds: number[] = []` sebelum loop pegawai
- Fetch `existingRecs` dengan field `id, employee_id, status, catatan, is_manual`
- Untuk pegawai yang `Tidak Aktif` (atau `tanggal_keluar < dateFilter`) yg punya record auto-generated → push id ke `staleAlphaIds`
- Setelah loop: `if (staleAlphaIds.length > 0) await supabase.from("attendance_records").delete().in("id", staleAlphaIds);`

**B2 — Per-division `denda_alpha`:**
- Sebelum: `const dendaAlpha = penalties.length > 0 ? (penalties[0]?.denda_alpha ?? 100000) : 100000;`
- Sesudah: `const penalty = penalties.find(p => p.division_id === empDivisionId);` + `computeDendaAlpha(penalty)`
- **⚠️ M12 fixup**: B2 fix berdasarkan asumsi salah bahwa `pegawai.division_id` exists. Setelah schema check via MCP, ternyata `pegawai` TIDAK punya FK ke `divisions` (hanya ke `jabatan` via `jabatan_id`, dan `jabatan` juga tidak punya `division_id`). Reverted ke first-penalty lookup (`penalties[0]`) untuk konsistensi dengan pre-M1 behavior. Field `division_id` di `EmployeeLite` juga dihapus (lihat M12).

**B3 — Multi-jenis leave:**
- Sebelum: `const leaveMap = new Map<string, { jenis: string, alasan: string }>();` (overwrite)
- Sesudah: `const leaveMap = new Map<string, { jenis: string, alasan: string }[]>().set(k, []).get(k).push(...);`
- Logic: kalau sudah ada approved leave → skip Alpha (tetap insert Izin/Sakit/Cuti, tapi pilih yg pertama).

### Verifikasi
- [x] `npm run lint` lulus tanpa error baru (hanya pre-existing react-hooks/purity warnings di page.tsx — out of scope)
- [ ] `npm run build` lulus (skipped untuk hemat waktu, typescript compile via next-build implicit)
- [x] `npm test` lulus — 43 tests passed (27 constants + 16 status)
- [x] `page.tsx` masih import dari `lib/*` bukan deklarasi lokal

### Tambahan M1 (di luar plan awal)
- Setup `vitest.config.ts` di root + npm script `test`/`test:watch`
- Tambah `AUTO_NOTE_PREFIXES` + `isAutoGeneratedNote()` ke constants untuk deteksi substring
- Tambah `STATUS_BY_VALUE` Map untuk O(1) lookup di lib/attendance-status.ts
- Refactor local `isAutoGeneratedCatatan` di page.tsx jadi thin wrapper ke `isAutoGeneratedNote`

### Rollback
Semua perubahan di M1 terisolasi di `attendance/page.tsx` + folder `lib/` + `__tests__/`.
Untuk rollback: hapus folder `lib/` & `__tests__/` di attendance/, revert `page.tsx`.

## M6 — Components: calendar + summary views

### Tujuan
Extract dua view mode (kalender matrix table + ringkasan per-pegawai) menjadi self-contained components supaya page.tsx cuma orchestrate view switching.

### File Baru
- `components/calendar-view.tsx` (203 LOC) — fullscreen matrix table (pegawai × tanggal) dengan:
  - Period navigator (prev/next month, cutoff 8-7)
  - Search filter nama pegawai
  - Status color legend
  - Body scroll lock saat mounted (preserved original behavior)
  - Self-fetch via `useCalendarData(periodKey)` (tidak butuh prop data)
- `components/summary-view.tsx` (349 LOC) — per-employee summary dengan:
  - Date mode toggle (periode vs custom range)
  - Period navigator + custom date picker
  - Search + sort pills (nama/hadir/alpha/telat/total)
  - 3-stat header (pegawai/total record/rata-rata hadir)
  - Tabel dengan sticky footer total
  - PDF + CSV export (dynamic import jspdf)
  - Self-contained: internal state + hooks

### File Diubah
- `page.tsx`:
  - Hapus 6 summary useState (dateMode, periodKey, customStart, customEnd, search, page, sortBy)
  - Hapus 4 useMemo (summaryPeriod, summaryDateLabel, summaryPaged, summaryTotals, summaryHeaderStats)
  - Hapus 4 handlers (exportCSV, exportPDF, navigateSummaryPeriod, summaryDateMode setter)
  - Hapus `useDropdown()` untuk summary export menu
  - Hapus `useSummaryData` + `useCalendarData` + `useCalendarFilters` imports
  - Hapus `getSummaryPeriodRange`/`getSummaryCurrentPeriodKey`/`getCalPeriod` imports
  - Hapus body scroll effect (dipindah ke `CalendarView`)
  - Hapus `calPeriodKey` state + sync effect
  - Update `reloadCal` callers (jadikan no-op — CalendarView fetch on mount)
  - Body scroll lock effect untuk `viewMode === "kalender"` dipindah ke `CalendarView`
- `components/off-day-modal.tsx`:
  - Hapus prop `calPeriodKey`
  - Tambah internal `useMemo` untuk compute `calPeriodKey` dari `new Date()`

### Key Decisions
- `CalendarView` & `SummaryView` sebagai plain components (no ref) — render conditional via `viewMode`
- Self-contained: own state, own data hooks (`useCalendarData` di-internal di CalendarView, `useSummaryData` di-internal di SummaryView), own dropdowns
- Minimal props: `CalendarView` butuh `employees` (untuk legend filter) + `onClose`; `SummaryView` butuh `employees` saja
- `useCalendarData` di `page.tsx` dihapus (CalendarView fetch on mount); `if (viewMode === "kalender") reloadCal()` jadi no-op — CalendarView mount-fresh setiap navigasi
- `OffDayModal` yang awalnya butuh `calPeriodKey` dari parent, sekarang derive sendiri (current month)

### Verifikasi
- [x] `npx tsc --noEmit` — 0 errors
- [x] `npm test -- --run` — 92/92 tests passing
- [x] `npm run lint` — 0 new errors di attendance (pre-existing warnings out of scope)
- [x] page.tsx 1.361 → 700 LOC (-661, -48.5%)

## M7 — Component: attendance table + hook

### Tujuan
Extract main table view (toolbar + table + pagination + export) ke component + hook supaya page.tsx cuma orchestrate (modal handlers, view switching, modals).

### File Baru
- `lib/hooks/use-attendance-table.ts` (104 LOC) — centralized state & logic untuk table view:
  - Wraps `useTableFilters` (search, filterStatus, page)
  - Wraps `useDropdown` untuk export menu
  - Computes `filtered`/`paged`/`statusCounts`/`totalDenda` (memoized)
  - Provides `exportCSV`/`exportPDF` (memoized, depend on `filtered` + `dateFilter`)
  - Returns flat object untuk di-spread ke `<AttendanceTable>` props dan dipakai page header (export button)
- `components/attendance-table.tsx` (213 LOC) — presentational table:
  - Toolbar: date navigator (prev/next day) + search box
  - Status filter pills (Semua + 7 status, dengan counts)
  - Denda badge (hanya jika > 0)
  - Table dengan 11 kolom (#/Pegawai/Divisi/Jam Masuk/Batas Telat/Jam Pulang/Status/Telat/Denda/Catatan/Aksi)
  - Pagination footer
  - Props minimal: records, derived data, callbacks (onEdit, onDelete, onDateChange)

### File Diubah
- `page.tsx`:
  - Hapus `useTableFilters` call → diganti `useAttendanceTable(records, dateFilter)` yang return `{ ...filters, filtered, paged, statusCounts, totalDenda, exportMenu, exportCSV, exportPDF }`
  - Hapus `useDropdown` call (exportMenu dari hook)
  - Hapus `exportCSV`/`exportPDF` functions (-54 LOC)
  - Hapus `statusCounts`/`totalDenda`/`filtered`/`paged` derived (-10 LOC)
  - Hapus `{viewMode === "tabel" && (<>...</>)}` block (-170 LOC JSX) → `<AttendanceTable ... />`
  - Header export button pakai `table.exportMenu` + `table.exportCSV`/`table.exportPDF`
  - `handleDelete` tetap di page (pakai `supabase` + `logAudit` + `reloadRecords`)

### Key Decisions
- `useAttendanceTable` hook sebagai single source of truth — page + AttendanceTable share state via hook
- Export button tetap di page header (paling prominent) — hook expose `exportMenu` + functions
- `AttendanceTable` sebagai pure presentational: tidak ada data fetching, tidak ada data computation
- `onDelete` callback terima full row (bukan cuma id) — page yang construct `deleteConfirm` object
- `onDateChange` callback — table tidak manage `dateFilter` (dimiliki page)

### Verifikasi
- [x] `npx tsc --noEmit` — 0 errors
- [x] `npm test -- --run` — 92/92 tests passing
- [x] `npm run lint` — 0 new errors di attendance
- [x] page.tsx 700 → 481 LOC (-219, -31.3%)

### Cumulative (M1-M7)
- page.tsx: 3.290 → 481 LOC (**-2.809, -85.4%**)
- New components: 6 (2.452 LOC total)
- New hooks: 1 (104 LOC) + 7 sebelumnya (368 LOC)
- New lib: 3 (320 LOC) + 7 hook files (368 LOC)
- Tests: 92/92 passing
- 0 TypeScript errors
- 0 new lint errors

## M8 — Components: small UI pieces

### Tujuan
Extract 4 small UI pieces yang masih inline di page.tsx: delete confirm modal, toast UI, view toggle, dan export menu button. Setelah M8, page.tsx murni jadi orchestrator (data + handlers + composition).

### File Baru
- `components/delete-confirm.tsx` (53 LOC) — generic delete confirmation modal:
  - `forwardRef<DeleteConfirmHandle>` + `useImperativeHandle` (sama pattern dengan modal lain)
  - Own state: `item` + `loading`
  - Props: `onConfirm(item)` callback
  - Imperative API: `ref.current?.open({ id, nama })`
- `components/toast-ui.tsx` (28 LOC) — presentational toast notification:
  - Props: `toast: ToastState` + `onDismiss: () => void`
  - Render Portal + animated card (success/error styling)
  - Auto-hide sudah di-handle oleh `useToast` hook
- `components/view-toggle.tsx` (30 LOC) — 3-button view mode toggle:
  - Props: `viewMode: AttendanceViewMode` + `onChange`
  - Tabel / Kalender / Ringkasan buttons
- `components/export-menu-button.tsx` (37 LOC) — header dropdown button:
  - Props: `menuRef`, `open`, `onToggle`, `onExportPDF`, `onExportCSV`, `disabled`
  - Generic untuk reuse di header page manapun

### File Diubah
- `page.tsx`:
  - Hapus `useState`/`setState` untuk `deleteConfirm` dan `deleting` → pakai `useRef<DeleteConfirmHandle>` + `ref.current?.open()`
  - Hapus inline Portal untuk delete confirm (~20 LOC) → `<DeleteConfirm ref={deleteConfirmRef} onConfirm={handleDelete} />`
  - Hapus inline Portal untuk toast UI (~17 LOC) → `<ToastUI toast={toast} onDismiss={dismissToast} />`
  - Hapus inline 3-button view toggle (~17 LOC) → `<ViewToggle viewMode={viewMode} onChange={setViewMode} />`
  - Hapus inline export menu dropdown (~21 LOC) → `<ExportMenuButton menuRef={...} open={...} ... />`
  - `handleDelete` jadi callback dengan param `item` (DeleteConfirm yang manage loading state)
  - Hapus unused imports: `useMemo`, `LayoutList`, `CalendarDays`, `BarChart3`, `Download`, `FileText`, `ChevronDown`, `Trash2`, `X`, `CircleCheckBig`, `AlertTriangle`, `cn`, `Portal`, `SUMMARY_PAGE_SIZE`, `SUMMARY_CUT_OFF_DAY`, `DAY_NAMES`, `STATUS_OPTIONS`, `STATUS_BY_VALUE`, `NO_JAM_STATUSES`, `MANUAL_SPECIAL`, `needsJamMasuk`, `isManualSelectable`, `type AttendanceStatus`, `addDays`, `getDeadlineTime`
  - Hapus unused state derivation: `statusCounts`, `totalDenda`, `filtered`, `paged` (sebelumnya di M7 sudah dihapus)

### Key Decisions
- `DeleteConfirm` sebagai generic component: `onConfirm(item)` callback, modal manage own loading state
- `ToastUI` sebagai thin wrapper: tidak manage state, hanya render
- `ViewToggle` pure presentational: props minimal
- `ExportMenuButton` generic: bisa dipakai di page lain (hanya butuh `menuRef` + handlers)
- Imperative ref pattern untuk DeleteConfirm (sama dengan modal lain) — lebih clean dari pada local state + conditional render

### Verifikasi
- [x] `npx tsc --noEmit` — 0 errors
- [x] `npm test -- --run` — 92/92 tests passing
- [x] `npm run lint` — 0 new errors di attendance
- [x] page.tsx 481 → 397 LOC (-84, -17.5%)

### Cumulative (M1-M8)
- page.tsx: 3.290 → 397 LOC (**-2.893, -87.9%**)
- New components: 10 (2.700 LOC total)
- New hooks: 1 (104 LOC) + 7 sebelumnya (368 LOC)
- New lib: 3 (320 LOC) + 7 hook files (368 LOC)
- Tests: 92/92 passing
- 0 TypeScript errors
- 0 new lint errors

## M9 — Hook: auto-generate libur + alpha

### Tujuan
Extract `autoGenerateLibur` dan `autoGenerateAlpha` (dua fungsi async panjang) + trigger `useEffect` ke dalam satu hook `useAttendanceAutoGen`. Setelah M9, page.tsx murni orchestrator: data loading, modal refs, composition. Tidak ada business logic di page.

### File Baru
- `lib/hooks/use-attendance-autogen.ts` (221 LOC) — side-effect hook:
  - Wraps kedua `useCallback` async functions
  - Wraps trigger `useEffect` (auto-run saat `staticDataReady` atau `dateFilter` berubah)
  - Args: `{ dateFilter, employees, penalties, publicHolidays, reloadRecords, staticLoading }`
  - Returns `void` (pure side effect)
  - Logic preserved 1:1 dari page.tsx (B1, B2, B3 bug fixes intact)

### File Diubah
- `page.tsx`:
  - Hapus `autoGenerateLibur` (107 LOC) + `autoGenerateAlpha` (138 LOC) + trigger `useEffect` (10 LOC) = -255 LOC
  - Tambah `useAttendanceAutoGen({ ... })` call (8 LOC)
  - Hapus imports: `useEffect`, `DEFAULT_DENDA_ALPHA`, `AUTO_NOTES`, `AUTO_NOTE_PREFIXES`, `computeDendaAlpha`
  - Hapus `useEffect` import (no other useEffect di page)
  - Net delta: **-216 LOC**

### Key Decisions
- Hook signature pakai object arg (named args) — lebih readable dari positional untuk 6 param
- `staticLoading` di-pass sebagai arg, hook compute `staticDataReady` internal
- Hook return `void` — pure side-effect (no return value), tidak expose internal state
- `// eslint-disable-next-line react-hooks/exhaustive-deps` dipindah ke hook (sesuai pattern existing)
- `useEffect` import di page.tsx dihapus (tidak ada useEffect lain di page)

### Verifikasi
- [x] `npx tsc --noEmit` — 0 errors
- [x] `npm test -- --run` — 92/92 tests passing
- [x] `npm run lint` — 0 new errors di attendance
- [x] page.tsx 397 → 205 LOC (-192, -48.4%)

### Cumulative (M1-M9)
- page.tsx: 3.290 → 205 LOC (**-3.085, -93.8%**)
- New components: 10 (2.700 LOC total)
- New hooks: 3 (462 LOC) + 6 sebelumnya (264 LOC)
- New lib: 3 (320 LOC)
- Tests: 92/92 passing
- 0 TypeScript errors
- 0 new lint errors

## M10 — Hook: delete attendance record

### Tujuan
Extract `handleDelete` callback (delete + audit log + toast + reload) ke hook `useAttendanceDelete`. Business logic delete terakhir yang masih ada di page.tsx — setelah M10, page murni orchestrator (no inline async business logic).

### File Baru
- `lib/hooks/use-attendance-delete.ts` (52 LOC) — encapsulates:
  - `supabase.from("attendance_records").delete()`
  - `logAudit({ action: "delete", ... })` dengan `oldData` snapshot
  - `showToast` success/error
  - `await reloadRecords()`
  - Returns memoized callback via `useCallback`

### File Diubah
- `page.tsx`:
  - Hapus `handleDelete` async function (18 LOC) + 2 unused imports (`supabase`, `logAudit`)
  - Tambah 1 import (`useAttendanceDelete`) + 1 line hook call
  - Net delta: **-17 LOC**

### Key Decisions
- Hook signature: `(records, reloadRecords, showToast) => useCallback(async (item) => {...})` — positional 3 args cukup ringkas
- `DeleteItem = { id: number; nama: string }` — typed parameter untuk `onConfirm` callback
- `oldData` snapshot dari `records.find(r => r.id === item.id)` — preserved 1:1 dengan logic sebelumnya
- ToastType re-imported dari `use-toast` (sudah exported)
- Dependencies array: `[records, reloadRecords, showToast]` — standar `useCallback` pattern

### Verifikasi
- [x] `npx tsc --noEmit` — 0 errors
- [x] `npm test -- --run` — 92/92 tests passing
- [x] `npm run lint` — 0 new errors di attendance
- [x] page.tsx 205 → 188 LOC (-17, -8.3%)

### Cumulative (M1-M10)
- page.tsx: 3.290 → 188 LOC (**-3.102, -94.3%**)
- New components: 10 (2.700 LOC total)
- New hooks: 4 (538 LOC) + 6 sebelumnya (264 LOC)
- New lib: 3 (320 LOC)
- Tests: 92/92 passing
- 0 TypeScript errors
- 0 new lint errors

### Final Architecture
```
page.tsx (188 LOC) — pure orchestrator
├── Data hooks: useAttendanceStaticData, useAttendanceRecords,
│               useAttendanceTable, useAttendanceAutoGen, useAttendanceDelete,
│               useViewMode, useToast
├── Modal refs: formModalRef, offDayModalRef, holidayDetailRef, deleteConfirmRef
├── Callbacks: openAdd, openEdit, openOffDay, handleDelete (1 line)
└── JSX composition: PageHeader, ToastUI, ViewToggle,
                     AttendanceTable / CalendarView / SummaryView,
                     AttendanceFormModal, OffDayModal, HolidayDetailModal,
                     DeleteConfirm, ExportMenuButton
```

## M11 — Composition: header + modals

### Tujuan
Compose related JSX chunks ke single-purpose wrapper components supaya page.tsx jadi declarative orchestrator. Setelah M11, page fokus ke flow logic (data + handlers + rendering), bukan menyusun 11 child components inline.

### File Baru
- `components/attendance-page-header.tsx` (66 LOC) — composes top-of-page chrome:
  - `<PageHeader>` dengan title/description/icon + actions (Atur Libur + Export menu + Input Absen)
  - `<ToastUI>` (portal-based toast notification)
  - `<ViewToggle>` (Tabel / Kalender / Ringkasan 3-button toggle)
  - Props: `viewMode`, `onViewModeChange`, `canInput`, `hasRecords`, `exportMenu`, `onExportPDF`, `onExportCSV`, `onOpenOffDay`, `onAddAbsen`, `toast`, `onDismissToast`
- `components/attendance-modals.tsx` (88 LOC) — composes semua 4 modal imperative:
  - `<AttendanceFormModal>` (form add/edit)
  - `<DeleteConfirm>` (delete confirmation)
  - `<OffDayModal>` (3-tab off day editor)
  - `<HolidayDetailModal>` (read-only holiday detail)
  - Props: 4 modal refs + `onDelete` + semua data props yang dibutuhkan form/offday + 2 saved callbacks + `onShowToast`

### File Diubah
- `page.tsx`:
  - Hapus 4 imports (`PageHeader`, `Button`, `ToastUI`, `ViewToggle`, `ExportMenuButton`, `AttendanceFormModal`, `OffDayModal`, `HolidayDetailModal`, `DeleteConfirm`)
  - Hapus inline `<PageHeader>` (~20 LOC) + inline actions dropdown (~13 LOC)
  - Hapus inline `<ToastUI>` (1 LOC) + `<ViewToggle>` (1 LOC) → `<AttendancePageHeader ... />` (1 LOC, 13 props)
  - Hapus inline 4 modals (~33 LOC) → `<AttendanceModals ... />` (1 LOC, 19 props)
  - Tambah 2 import: `AttendancePageHeader`, `AttendanceModals`
  - Net delta: **-49 LOC**

### Key Decisions
- `AttendancePageHeader` fokus ke top-of-page chrome (PageHeader + Toast + ViewToggle) — bukan container untuk seluruh page
- `AttendanceModals` sebagai composition wrapper — props forwarding pattern, bukan abstraction (tetap expose semua child props)
- Modal refs di-pass via prop (`RefObject<...Handle | null>`) — type preserved, parent owns ref lifecycle
- `onFormSaved`/`onOffDaySaved` callbacks tetap di page (mereka orchestrate reloads: `reloadStatic()` + `reloadRecords()`)
- Wrapper tidak encapsulate business logic — murni grouping JSX

### Verifikasi
- [x] `npx tsc --noEmit` — 0 errors
- [x] `npm test -- --run` — 92/92 tests passing
- [x] `npm run lint` — 0 new errors di attendance
- [x] page.tsx 188 → 139 LOC (-49, -26.1%)

### Cumulative (M1-M11)
- page.tsx: 3.290 → 139 LOC (**-3.151, -95.8%**)
- New components: 12 (3.077 LOC total)
- New hooks: 4 (538 LOC) + 6 sebelumnya (264 LOC)
- New lib: 3 (320 LOC)
- Tests: 92/92 passing
- 0 TypeScript errors
- 0 new lint errors

### Final Architecture (post-M11)
```
page.tsx (139 LOC) — pure orchestrator
├── Data hooks: useAttendanceStaticData, useAttendanceRecords,
│               useAttendanceTable, useAttendanceAutoGen, useAttendanceDelete,
│               useViewMode, useToast
├── Modal refs: formModalRef, offDayModalRef, holidayDetailRef, deleteConfirmRef
├── Callbacks: openAdd, openEdit, openOffDay, handleDelete
├── JSX (semantic): <AttendancePageHeader> (chrome)
│                    <AttendanceTable> / <CalendarView> / <SummaryView> (per-view)
│                    <AttendanceModals> (4 modals)
└── Semua business logic di hook/component — page pure declarative
```

## M13 — Fix calendar default period: gunakan cutoff-aware period key (bugfix)

### Bug discovery (user-reported)
Mode kalender default ke periode "8 Juni – 7 Juli" padahal hari ini 5 Juni 2026, yang berarti masih di periode "8 Mei – 7 Juni" (cutoff 8-7, June 5 < 8 → sebelumnya).

### Root cause
- `CalendarView` punya local helper `getCurrentMonthKey()` yang return `YYYY-MM` dari current month
- Untuk June 2026 → "2026-06" → `getCalPeriod("2026-06")` = 8 Juni – 7 Juli (next period!)
- Seharusnya: gunakan `getSummaryCurrentPeriodKey()` (sudah exists di `attendance-helpers.ts`) yang aware cutoff day

### Fix
- `components/calendar-view.tsx`:
  - Import `getSummaryCurrentPeriodKey` dari `attendance-helpers`
  - Hapus local `getCurrentMonthKey()` (redundant)
  - `useState(getSummaryCurrentPeriodKey)` — default ke periode yang mengandung hari ini
- `lib/attendance-helpers.ts`:
  - `getSummaryCurrentPeriodKey` refactor: terima optional `date: Date` param (default `new Date()`) supaya bisa ditest deterministically
- `__tests__/attendance-helpers.test.ts`:
  - Tambah 9 test untuk `getSummaryCurrentPeriodKey` covering:
    - Sebelum cutoff (5 Juni → "2026-05")
    - Tepat cutoff (8 Juni → "2026-06")
    - Setelah cutoff (10 Juni → "2026-06")
    - 1 Juli → "2026-06"
    - 7 Juli → "2026-06"
    - Rollover tahun (1 Jan 2026 → "2025-12")
    - Setelah cutoff (15 Jan → "2026-01")
    - Akhir tahun (31 Des → "2026-12")
    - Default no-arg (tidak throw, format YYYY-MM)

### Verifikasi
- [x] `npx tsc --noEmit` — 0 errors
- [x] `npx vitest run` — 101/101 passing (was 92; +9 new tests for getSummaryCurrentPeriodKey)
- [x] Manual: refresh browser → kalender default ke 8 Mei – 7 Juni ✓

### Lessons learned
- Test coverage untuk period/cutoff logic penting — function `getSummaryCurrentPeriodKey` sudah exists sejak lama tapi tidak punya test, sehingga bug missed saat M6 extraction
- Saat extract hook/component, **sertakan existing helpers + tests**, jangan duplicate logic dengan versi yang lebih naïf

## M12 — Fix regression: `pegawai.division_id` tidak exists (bugfix hotfix)

### Bug discovery
User laporan: mode kalender muncul "Tidak ada pegawai sesuai pencarian". Mode table tetap muncul data.

### Root cause
- M3 refactor menambahkan `division_id: number | null` ke `EmployeeLite` type dan ke `SELECT` di `use-attendance-static-data.ts`
- B2 fix (M1) pakai `emp.division_id` untuk per-division `denda_alpha` lookup
- **Asumsi salah**: ternyata `pegawai` table TIDAK punya kolom `division_id`. Schema check via Supabase MCP:
  - `pegawai` FKs: `jabatan_id → jabatan.id`, `recruitment_id → recruitments.id` (TIDAK ada `divisions`)
  - `jabatan` juga TIDAK punya `division_id`
  - Direct link `pegawai → divisions` tidak ada
- Query `.from("pegawai").select("...division_id...")` return error `column pegawai.division_id does not exist`
- Karena Promise.all, error di salah satu query menyebabkan `empRes.data = null` → `employees = []` (default)
- Calendar view empty (tergantung `employees` dari static data)
- Table view tetap works (query langsung `attendance_records` JOIN `pegawai` via FK di `attendance_records.employee_id → pegawai.id`, tidak butuh static `employees`)

### Bug tidak ke-detect saat M3 karena:
- `useAttendanceRecords` query `attendance_records` (bukan `pegawai` langsung) — tidak expose error
- `useAttendanceAutoGen` cuma log error (suppress), tidak throw
- Tidak ada test untuk `useAttendanceStaticData` (deferred — butuh DOM env)
- Pre-check M1 dengan SQL: tidak cek apakah kolom `division_id` ada di `pegawai`

### Fix
- Hapus `division_id: number | null` dari `EmployeeLite` type
- Hapus `division_id` dari `SELECT` di `useAttendanceStaticData` (kembali ke fields: `id, nama, status, tanggal_bergabung, tanggal_keluar`)
- `use-attendance-autogen.ts`: revert B2 per-division lookup ke `penalties[0]` (first penalty, fallback to default) — match pre-M1 behavior. TODO future: lookup division dari most recent `attendance_records` per employee
- Hapus debug `console.log` yang ditambahkan saat investigasi

### Verifikasi
- [x] `npx tsc --noEmit` — 0 errors
- [x] `npm test -- --run` — 92/92 tests passing
- [x] Manual test: calendar view sekarang muncul list pegawai, table view tetap works

### Lessons learned
- **Schema assumption check** harus jadi part of refactor planning — query column existence sebelum add ke type/SELECT
- **Hook error logging**: `useAttendanceStaticData` silently swallows error (Promise.all + default). Untuk future, pertimbangkan expose error di return value
- **Missing test coverage**: hooks tidak punya unit test — bug tersembunyi. Setup jsdom/happy-dom + testing-library jadi higher priority setelah ini
